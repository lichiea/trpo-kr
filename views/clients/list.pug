extends ../layout

block content
    div
        ul
          li
            a(href='/') Главная страница
    h1= title
    table
        tr 
            th id
            th Имя
        each item in clients
            tr
                td= item.id
                td= item.label
